<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/public/css/style.css">
  <title>RepliTube - Dashboard</title>
</head>
<body>
  <header>
    <h1>RepliTube Dashboard</h1>
  </header>
  <div class="container">
    <div class="profile">
      <img src="<%= user.profilePicture %>" alt="Profile Picture" width="50">
      <h3>Welcome, <%= user.name %>!</h3>
    </div>
    <h2>Your YouTube Channel: <%= user.channelName %></h2>
    <form action="/upload/video/<%= user.id %>" method="POST" enctype="multipart/form-data">
      <input type="text" name="title" placeholder="Video Title" required>
      <textarea name="description" placeholder="Video Description" rows="5" required></textarea>
      <input type="file" name="video" accept="video/mp4" required>
      <button type="submit">Upload Video</button>
    </form>
    <% if (error) { %>
      <div class="error"><%= error %></div>
    <% } %>
  </div>
</body>
</html>